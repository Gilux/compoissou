{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
    <h1>Signalement article creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input class="btn btn-info" type="submit" value="Créer" />
    {{ form_end(form) }}

    <a class="btn btn-warning" href="{{ path('signalementarticle_index') }}">Retourner à l'article</a>

{% endblock %}
